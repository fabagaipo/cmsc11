#include <stdio.h>
#include <string.h>
//Type the program and write down
//what's wrong when running the program
int main (void) {
    char firstChar, secondChar;

    printf("What character comes after a? ");
    scanf(" %c", &firstChar);

    printf("What character comes after y? ");
    scanf(" %c", &secondChar);

    return 0;
}